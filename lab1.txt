#include <iostream>
#include <bitset>
#include <locale>

using std::cin;
using std::cout;


int main()
{
    // zadanie 1
    std::cout << "1." << "\n";
    std::cout <<"Efremov Nikita Alexeyevich, 221-331" << "\n";
    
    // zadanie 2
    std::cout << "2." << "\n";
    
    
    
    
    // zadanie 3
    std::cout << "3." << "\n";
    std::cout<<"Enter a number:"<< "\t";
    int f;
    std::cin >>f;
    std::cout << "In binary form" << "\t" << std::bitset<16>(f) << "\n" 
    << "In hexadecimal form" << "\t" << std::hex << f << "\n"
    << "bool" << "\t" << bool(f) << "\n" 
    << "double" << "\t" << double (f) << "\n" 
    << "char" << "\t" << char (f) << "\n";
    
    // zadanie 4
    std::cout << "4." << "\n";
    int a,b;
    std::cin >> a >> b;
    std::cout << "Enter the coefficients a * x = b: " << a << b << "\n";
    std::cout << a << " * x = " <<  b << "\n";
    std::cout << "x = " << b << "/" << a << "\n";
    std::cout << "x = " << b/a << "\n";
    std::cout << "Answer:" << b/a << "\n";
    
    
    // zadanie 5
    std::cout << "5." << "\n";
    std::cout << "Enter the coordinates of the segment on the straight line" << "\t";
    int q,w;
    std::cin >> q;
    std::cin >> w;
    std::cout << "The middle of the segment is at the point with the coordinate" << "\t";
    std::cout << (q + w) / 2;
    
    return 0;
}